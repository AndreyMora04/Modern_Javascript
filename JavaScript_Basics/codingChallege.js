//Basic Operators and first coding challenge:

var maxHeight = 1.90;
var maxMass = 90;
var maxBMI = (maxMass) / (maxHeight * maxHeight);


var jonhHeight = 1.80;
var jonhMass = 80;
var jonhBMI = (jonhMass) / (jonhHeight * jonhHeight);

if(maxBMI > jonhBMI){

alert('Is Max BMI hihger than Jonh?' + ' ' + true)

}else{

alert('Sorry :c')

}
